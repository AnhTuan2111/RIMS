server:
  port: 8080
spring:
  application:
    name: rims-api
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=RIMS_DB;encrypt=true;trustServerCertificate=true
    username: sa
    password: REMOVED-DB-PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
jwt:
  signerKey: "REMOVED-JWT-SIGNER-KEY"

vnpay:
  url: "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
  tmn-code: "D90AVGT4"
  hash-secret: "REMOVED-VNPAY-HASH-SECRET"
  version: "2.1.0"
  command: "pay"
  return-url: "http://localhost:8080/rims/cashier/payments/vnpay-callback"