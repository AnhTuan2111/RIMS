spring:
  datasource:
    url: jdbc:h2:mem:rims_test;MODE=MSSQLServer;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: REMOVED-DB-PASSWORD
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
jwt:
  signerKey: REMOVED-JWT-SIGNER-KEY
