spring:
  application:
    name: rms-api
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=RMS_DB;encrypt=true;trustServerCertificate=true
    username: sa
    password: REMOVED-DB-PASSWORD
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: true
jwt:
  signerKey: REMOVED-JWT-SIGNER-KEY
