spring:
  datasource:
    url: jdbc:h2:mem:rms_test;MODE=MSSQLServer;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
jwt:
  signerKey: test-signer-key-for-unit-tests-only-32chars
